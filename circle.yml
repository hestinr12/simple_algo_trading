machine:
  python:
    version: 3.4.2

dependencies:
  post:
    - git clone https://github.com/blampe/IbPy
    - ls
    - cd IbPy/
    - ls
    - python setup.py install
    - cd ..

test:
  override:
    - python tests.py
