machine:
  python:
    version: 3.4.2

dependencies:
  post:
    - git clone https://github.com/blampe/IbPy
    - cd IbPy/
    - python setup.py install
    - cd ..

test:
  override:
    - python tests.py
